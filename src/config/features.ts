/**
 * 학기 중에 켜고 끄는 스위치.
 *
 * 여기 있는 값은 코드를 바꿔 다시 배포해야 적용된다 —
 * 선생님이 교사 화면에서 누를 수 있는 설정이 아니다.
 * 수업 중에 자주 바꿔야 하는 것이 생기면 교사 화면으로 옮기는 게 맞다.
 */

/**
 * 학생이 스스로 새 이름을 만들 수 있는지. (요청 1)
 *
 * 학기 초에 반 아이들 이름을 다 만든 뒤에는 끈다. 켜 두면 장난으로 이름이 계속
 * 늘어나 목록에서 자기 이름을 찾기 어려워지고, 지운 이름의 진행도도 되돌릴 수 없다.
 *
 * 끄면 이름 목록만 보여 준다. 목록이 비어 있어도 만들기 화면으로 넘어가지 않고
 * 선생님을 부르라고 안내한다 — 학생이 스스로 빠져나갈 길이 없어야 한다.
 */
export const ALLOW_NEW_PROFILE = false;
