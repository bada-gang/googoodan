# ART_DIRECTION.md — 구구 마을 아트 가이드

> 새 에셋을 만들기 전에 **반드시** 이 문서를 먼저 읽는다. (명세 66)
>
> 이 문서의 수치는 코드에서 `src/config/artTokens.ts` 로 고정되어 있다.
> 문서와 코드가 다르면 코드가 기준이며, 코드를 고칠 때 이 문서도 함께 고친다.

---

## 1. 게임 분위기

따뜻한 생활형 게임. 밝고 편안하며, 아기자기하지만 유아용처럼 보이지 않는다.
초등학교 2학년이 "계속 집과 농장을 꾸미고 싶어지는" 느낌이 목표다.

**목표 레퍼런스 감각**: 부드러운 2D 일러스트형 게임 그래픽 — 둥근 형태, 단순하지만
명확한 실루엣, 부드러운 외곽선, 적당한 디테일.

**금지**: 사실적 렌더링, 픽셀 아트, 3D, 플랫 디자인 아이콘 스타일, 이모지.

---

## 2. 캐릭터 비율

```
머리 : 몸 = 1 : 1.5 ~ 2
전체 키 = 132px (논리 해상도 1280×800 기준)
머리 높이 = 전체의 약 40%
```

지나치게 머리가 큰 SD 비율은 쓰지 않는다.
동물은 현실 비율보다 약간 귀엽게 과장하되, 종별 실루엣이 확실히 구분되어야 한다.

---

## 3. 기본 팔레트

전체적으로 밝고 따뜻하게. 채도가 높은 원색은 **상호작용과 보상에만** 쓴다.

| 용도 | 색 |
| --- | --- |
| 하늘 위 / 아래 | `#bfe6f5` / `#e8f6fb` |
| 잔디 (밝음/기본/그늘) | `#bde184` / `#a8d66f` / `#8bbd58` |
| 흙 (밝음/기본/그늘) | `#bd8b5e` / `#a9764c` / `#8b5e3a` |
| 잎 (밝음/기본/그늘) | `#8ed486` / `#6fbf6a` / `#529c50` |
| 나무 줄기 | `#a3714a` (그늘 `#84593a`) |
| 목재 (밝음/기본/어두움) | `#d8a973` / `#c08a52` / `#9a6a3c` |
| 벽 / 지붕 | `#f7e6cd` / `#e07f63` |
| 크림 (UI 바탕) | `#fff6e2` |
| 금색 (돈·보상) | `#f6c453` (그늘 `#dba63a`) |
| 강조 — 분홍 / 민트 / 하늘 / 보라 | `#f6a8bd` / `#8fd8c4` / `#7fb8e6` / `#b8a0e0` |
| 외곽선 | `#4a3a2f` (연한 외곽선 `#6a5445`) |

열매: 사과 `#e8524a` · 오렌지 `#f2953a` · 복숭아 `#f7a3a0` · 체리 `#d1385c`
포도 `#8f6fc4` · 감 `#ea8034`

---

## 4. 외곽선 규칙

굵기는 오브젝트 크기와 상관없이 **3단계만** 사용한다.

```
thin  = 2px   작은 디테일 (눈, 잎, 손잡이)
base  = 3px   일반 오브젝트
bold  = 4px   큰 오브젝트 (건물, 나무 수관, 가구 몸체)
```

외곽선 색은 항상 `#4a3a2f`. 검정(`#000`)은 쓰지 않는다.
선 끝과 이음은 둥글게(`round`) 처리한다.

---

## 5. 명암 규칙

단계는 **2~3단계**만. 그라데이션 블러나 사실적 조명은 쓰지 않는다.

```
1) 기본색
2) 그늘색   = 기본색을 18~32% 어둡게 (반투명 오버레이 알파 0.3~0.45)
3) 하이라이트 = 기본색을 18~40% 밝게 (알파 0.4~0.55, 왼쪽 위에만)
```

---

## 6. 광원 방향

```
광원   : 왼쪽 위
그림자 : 오른쪽 아래
```

모든 에셋에서 동일하게 유지한다. 오브젝트마다 그림자 방향이 바뀌면 안 된다.
접지 그림자는 오브젝트 바닥에 타원으로, 오른쪽으로 반지름의 약 8%만큼 밀어 그린다.
색은 `rgba(74, 58, 47, 0.22)`.

---

## 7. 오브젝트 기준 크기

논리 해상도 1280×800 기준 픽셀. 바닥선(`GROUND_Y`)은 y = 660.

| 대상 | 프레임 크기 | 비고 |
| --- | --- | --- |
| 집 | 420 × 360 | 원점 = 바닥 중앙 |
| 상점 (농장 가게/가구 가게/게임) | 340 × 330 | 원점 = 바닥 중앙 |
| 헛간 | 380 × 330 | |
| 여물통 | 240 × 140 | |
| 나무 (5단계) | 260 × 300 × 5프레임 | 6종 (사과·오렌지·복숭아·체리·포도·감) |
| 밭(흙) | 220 × 90 | |
| 울타리 1칸 | 160 × 130 | 158px 간격으로 이어 붙임 |
| 표지판 | 170 × 190 | |
| 실내 창문 / 문 | 200 × 180 / 150 × 240 | |

---

## 8. 캐릭터 기준 크기

```
프레임      : 120 × 160
캐릭터 키   : 132
원점        : (0.5, 1)  발바닥 중앙
idle        : 4프레임 / 4fps
walk        : 6프레임 / 11fps
interact    : 4프레임 / 10fps (1회 재생)
happy       : 4프레임 / 9fps  (1회 재생)
```

오른쪽을 보는 그림만 만든다. 왼쪽은 `flipX` 로 처리한다.

---

## 9. 동물 기준 크기

| 동물 | 프레임 | 몸 높이 |
| --- | --- | --- |
| 닭 | 96 × 96 | 62 |
| 염소 | 120 × 112 | 78 |
| 양 | 128 × 112 | 86 |
| 젖소 | 160 × 140 | 116 |

각 동물은 `idle / walk / eat / happy` 4종을 4프레임씩 만든다.
염소는 갈색 계열 + 뒤로 휜 뿔 + 턱수염으로 흰 털의 양과 구분한다.
**idle 에서도 반드시 움직인다** — 눈 깜빡임, 꼬리 흔들기, 고개 돌리기 중 하나 이상.

---

## 10. 가구 기준 크기

가구는 실제 비율보다 **약간 크게** 만든다. 태블릿에서 고르기 쉽고 꾸미기 변화가 잘 보인다.

```
기본 단위     : 96
의자·쿠션류   : 76 ~ 116
책상·탁자류   : 150 ~ 160
침대·소파류   : 210 ~ 220
장(높은 가구) : 높이 190 ~ 230
```

원점은 `(0.5, 1)` 바닥 중앙. 작은 썸네일(54~62px)에서도 형태가 구별되어야 한다.
가구 목록과 실제 수치는 `src/config/catalog.ts` 의 `FURNITURE` 를 따른다.

---

## 11. UI 버튼 규칙

| 항목 | 값 |
| --- | --- |
| 최소 터치 영역 | 48 × 48 |
| 주요 행동 버튼 | 최소 높이 76 |
| 이동 버튼 | 112 × 96 |
| 버튼 테두리 | 4px `#4a3a2f` |
| 버튼 모서리 | 20px |
| 눌림 효과 | 아래로 5px 이동 + 그림자 축소 |
| 본문 최소 글자 | 1rem (16px, 논리 해상도 기준) |
| 문제 글자 | 4.4rem |
| 선택지 글자 | 2.8rem |

UI 는 웹사이트가 아니라 **게임 HUD** 처럼 보여야 한다.
나무 간판(`.panel-wood`), 종이 메모(`.panel-paper`) 형태를 쓰고,
SaaS 대시보드 카드·Bootstrap 모달·네비게이션 바 형태는 쓰지 않는다.

---

## 12. 애니메이션 프레임 규칙

```
반복 애니메이션(잎 흔들림, 구름, 간판) : 2.2 ~ 5.2초, Sine.easeInOut, yoyo
정답/보상 연출                          : 0.5 ~ 1초 안에 끝낸다
상호작용 피드백(팝인)                   : 0.26초, Back.easeOut
오답 흔들림                             : 0.43초, 좌우 7px 이내
```

모든 오브젝트가 동시에 크게 움직이지 않게 한다. 흔들림 각도는 0.003 ~ 0.02 라디안.

---

## 13. 금지 스타일

1. 최종 그래픽에 이모지 사용
2. HTML/CSS 도형으로 집·나무·캐릭터 표현
3. 픽셀 아트 + 벡터 + 사진 등 서로 다른 스타일 혼합
4. 오브젝트마다 다른 광원 방향
5. 지나친 그라데이션, 사실적 그림자, 블러
6. 화면 전체를 덮는 파티클
7. 강한 빨강 번쩍임이나 실패음 (오답 연출에 특히 금지)
8. 둥근 흰색 카드가 화면을 가득 채우는 SaaS UI
9. 작아서 읽기 어려운 글자
10. placeholder 에셋을 최종 에셋처럼 취급하기

---

## 14. 임시(placeholder) 아트에서 실제 아트로 교체하는 방법

현재 모든 그림은 `src/game/placeholder/` 에서 Canvas 로 그려 만든 **임시 에셋**이다.

1. `src/config/assets.ts` 의 `ASSET_MANIFEST` 에서 해당 키의 `path` 와 규격을 확인한다.
2. 그 경로대로 `public/assets/` 아래에 PNG 를 넣는다.
   예) `player_walk` → `public/assets/characters/player_walk.png` (120×160 × 6프레임)
3. `public/assets/art-manifest.json` 의 `ready` 배열에 그 키를 추가한다.

```json
{ "ready": ["player_idle", "player_walk", "tree_apple"] }
```

4. 새로고침하면 그 키만 실제 파일에서 로드되고, 나머지는 계속 임시 아트로 동작한다.
   게임 코드는 한 줄도 바꾸지 않는다.

교체가 끝났는지 확인하려면 설정 화면 하단의 "임시 아트" 안내 문구가 사라졌는지 본다.

### AI 로 그래픽을 만들 경우 (명세 65)

개별 이미지를 무작위로 하나씩 만들지 않는다. 먼저 아래를 고정하고 일괄 제작한다.

- 캐릭터 비율 (2번 항목)
- 외곽선 굵기와 색 (4번 항목)
- 명암 단계 (5번 항목)
- 카메라 시점 — 약간 위에서 내려다보는 2.5D 측면
- 색상 팔레트 (3번 항목)
- 배경 투명 (PNG, 알파 채널)
- 기준 해상도 (7~10번 항목)
